import React, { FC } from 'react';
import { BsDot } from 'react-icons/bs';

import colors from '@styles/colors';

const Dot: FC = () => <BsDot size={55} color={colors.secondary} />;

export default Dot;
